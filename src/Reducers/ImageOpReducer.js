import { FETCH_IMG, FETCH_IMG_SUCCESS, FETCH_IMG_FAILED, ADD_IMG_FRM_PROP } from "../utils/constants";

const INITIAL_STATE = {

}

export default ImageOpReducer = (state = INITIAL_STATE, action) => {

    switch (action.type) {
        case FETCH_IMG:

            return state;

        case FETCH_IMG_SUCCESS:
            
            return state;

        case FETCH_IMG_FAILED:
            
            return state;
        
    }

}